// Global
const HOME = "/";

// Note
const NOTE = "/note";

//user
const LOGIN = "/login";
const JOIN = "/join";

//kakao
const KAKAO = "/auth/kakao";
const KAKAO_CALLBACK = "/oauth";

const routes = {
  home: HOME,
  note: NOTE,
  login: LOGIN,
  join: JOIN,
  kakao: KAKAO,
  kakakoCallback: KAKAO_CALLBACK,
};

export default routes;
