// Global
const HOME = "/";

// Note
const NOTE = "/note";

//user
const LOGIN = "/login";
const JOIN = "/join";

const routes = {
  home: HOME,
  note: NOTE,
  login: LOGIN,
  join: JOIN,
};

export default routes;
