main 
    nav
        a.service_name(href=routes.home) 쿼카 
        a.note(href=routes.note) 상담 일기
        .user_login
            a.login(href=routes.login) 로그인
            a.join(href=routes.join) 회원가입

